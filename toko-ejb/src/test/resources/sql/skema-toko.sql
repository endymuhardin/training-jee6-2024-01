create table produk (
    id varchar(36),
    primary key (id)
);

create table pelanggan (
    id varchar(36),
    primary key (id)
);

create table pembelian (
    id varchar(36),
    primary key (id)
);

create table pembayaran (
    id varchar(36),
    primary key (id)
);